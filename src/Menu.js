import React from 'react'

const Menu = ({menuData}) => {
    return (
        <div className="section-center">
            {menuData.map((item) => {
                return (
                    
                )
            })}
        </div>
    ) 
}

export default Menu
